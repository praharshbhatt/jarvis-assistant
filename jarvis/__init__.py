# Define what should be considered as the package's public interface.
# This file is executed when the package is imported, and it often contains a list of modules and symbols
# (functions, classes, etc.) that are meant to be accessible from outside the package.

from .ai_model import AIModel